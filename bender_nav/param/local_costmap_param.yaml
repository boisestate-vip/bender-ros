local_costmap:
  global_frame: odom
  robot_base_frame: body
  update_frequency: 5.0
  publish_frequency: 2.0
  width: 6.0
  height: 6.0
  resolution: 0.05
  rolling_window: true
  plugins:
    - {name: static_layer,            type: "costmap_2d::StaticLayer"}
    # - {name: obstacles_layer,         type: "costmap_2d::VoxelLayer"}
    # - {name: sonar,                   type: "range_sensor_layer::RangeSensorLayer"}
    # - {name: inflation_layer,         type: "costmap_2d::InflationLayer"}
  # obstacles_layer:
    # observation_sources: laser_scan_sensor
    # laser_scan_sensor: {sensor_frame: d435_depth_frame, data_type: LaserScan, topic: /scan, marking: true, clearing: true}
  # sonar:
    # topics: ['/pi_sonar/sonar_0', '/pi_sonar/sonar_1', '/pi_sonar/sonar_2', '/pi_sonar/sonar_3', '/pi_sonar/sonar_4']
  # inflation_layer:
  #   enabled:              false
  #   cost_scaling_factor:  10.0   # exponential rate at which the obstacle cost drops off (default: 10)
  #   inflation_radius:     0.55   # max. distance from an obstacle at which costs are incurred for planning paths.